const samplesReducer = (state=[], action) => {
    switch (action.type){
        case "CURRENT_USER":
            
        default:
            return state
    }
    
}

export default samplesReducer